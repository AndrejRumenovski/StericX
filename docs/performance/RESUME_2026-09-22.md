# Paused optimization checkpoint — 2026-09-22

Paused at the user's request to continue tomorrow. No benchmark, build or
reference-replay jobs remain active. The goal is unfinished. Do not restart the
scientific remediation or repeat completed C1 checks without a new reason.

## Published corrected baseline

Scientific remediation is committed and pushed as
`1ecfbd5be8b354711bdadd8a8d148417e03d8e7a`. Its scientific admission is scoped;
the documented numerical, methodological and predictive limits still apply.
Read `docs/scientific_remediation/SCIENTIFIC_REMEDIATION.md`, `ADMISSION.json`
and the performance reports before continuing.

All paths below are relative to
`.stericx/profiling/scientifically_validated_optimization/` unless stated otherwise.

- Immutable baseline: `accurate_baseline_v1/`; manifest SHA-256
  `feb021566f4a172c8d249184d1e16e39978b59c1784d216f4f50c9261ed27ced`.
- Baseline executable SHA-256:
  `102b6883c639bfc9ef211a88abae02391419cbd277c498098fa2bdfc7008206f`.
- Fixed workloads: `corrected_workloads_v2/workloads/manifest.json`.
- Actual scientific oracle: `scientific_oracle_v3/manifest.json`.
  Derive this from the frozen build; the example `requests_v1` path does not exist.
- Fresh native and diagnostic baselines cover all ten workloads at 1/2/4/6
  threads. See `CORRECTED_PROFILE.md` and `CORRECTED_COUNTERS.md`.

Never overwrite this baseline, original audit evidence or failed attempts.

## C1: implemented and verified; root acceptance pending

The only production change is ordered file parallelism in `src/descriptors.rs`.
Per-file scientific calculations and conformer reductions remain unchanged.
Tests are in `tests/descriptor_batch_parallel.rs`. These optimization changes
and their reports are still uncommitted. User-owned `docs/media/` is untracked
and must not be edited or staged.

Evidence:

- Proposal/freeze: `candidate_c1_ordered_files/` and
  `docs/performance/candidates/C1_ORDERED_FILES.md`.
- Build: `candidate_c1_science_v2/validated_build_corrected_vC1/manifest.json`.
  Native SHA-256:
  `2f65e0aba46a502bb653c61ac3090fa0acbb0244dedcf4aba18ce06e458be358`.
- Exact oracle: all 128,021 responses in 13 lanes match; 93 CLI cases match at
  each thread count. Engineering checks pass: 307 Rust tests, 124 Python tests,
  Clippy, rustdoc, Rust formatting and Ruff. Supplemental reference-integrity
  checks also pass.
- Full independent references were rerun, including 31,721 conformers,
  1,141,956 regional bins and 7,624 model comparisons. Use the resolved gate at
  `candidate_c1_science_v2/independent_reference_resolution_v1/independent_gate.json`
  (SHA-256 `6f15eb8052b8612b0f5de67f10be60faeafbc4085e6182e259b43db74caa7f96`).
  Preserve the original failed gate: six model-hash fields changed solely because
  regenerated models had a different `created_utc`. The additive resolution
  proves identical model content after the already-declared timestamp exclusion;
  no scientific value or tolerance changed.
- Native/diagnostic prediction exports are bit-identical to all one million
  frozen f32 values. See `candidate_c1_diagnostics_v1/prediction_comparison.json`.
- Combined pre-benchmark admission: `candidate_c1_performance_gate_v1/manifest.json`.
- Full native alternating A/B: `candidate_c1_native_pairs_v1/manifest.json`,
  SHA-256 `0bc562d34ee3791821889749f5245ef659dcc4a5c3962d05646085cd313f3ed5`.
  All 40 configurations and 720 launches completed with pre/post verification.
- Focused repeat: `candidate_c1_native_supplement_v1/manifest.json`, SHA-256
  `624bdf0051d72e722ec64b4ed505fe80d2032e6c0b26f5d2c3f140decd0bd571`.
  Five configurations, 24 measured pairs each, 250 launches; all retained.
- Independent performance review: `candidate_c1_independent_review_v1/review.json`,
  SHA-256 `cff04dac6a2537ac39ef873097020536b6c29515d5f4b1391b38eb82e4fc4724`.
  Recommendation: **accept with explicit batch-focused tradeoffs**.

Paired median speedups for 10,000 files are 1.931×, 3.693× and 5.287× at 2/4/6
threads. Six-thread median wall time is 4.970 s baseline versus 0.940 s candidate.
The 56-file workload gains 1.804×, 2.993× and 4.051×. This is file parallelism,
not parallelization within a single conformer ensemble.

Tradeoffs must remain explicit. The repeat confirms approximately 2.28%/2.01%
parser wall slowdown at 1/2 threads. Screening wall time is approximately neutral
in the repeat, while CPU time rises about 1.75%/1.84%. Search CPU rises about
4.65%; its highly variable wall measurements do not support a precise wall-time
claim. Six-thread batch peak RSS reaches 26.4 MiB, about 5.6 MiB above baseline,
and total CPU work increases. No samples were excluded. Do not claim universal
speedup or unchanged unrelated performance.

## Resume sequence

1. Review the sealed C1 recommendation and record root's acceptance decision.
   No root acceptance receipt has been written yet. The prepared reprofile
   controller requires `kind=accepted_corrected_optimization`, `accepted=true`
   and file records for `candidate_build`, `paired_benchmark_manifest`,
   `candidate_gate` and `paired_summary`. Bind the supplemental review as well.
2. Run the prepared post-C1 diagnostic reprofile in a quiet window. Invocation
   and interpretation rules are in `C1_REPROFILE.md`; controller is
   `candidate_reprofile_controller_v1/run.py`. Use new output
   `accepted_c1_reprofile_v1`. It reuses all eight actual native samples and
   runs 40 diagnostic configurations without rebuilding.
3. If the fresh profile supports it, freeze and implement C2 only: lazy reuse of
   the immutable model's Student-t multiplier during one screen invocation.
   Proposal: `candidates/C2_QUANTILE_REUSE_PROPOSAL.md`. Preserve actual public
   API behavior, including unchecked zero-parameter metadata; CLI model loading
   already rejects that metadata. Do not add new validation inside this
   optimization or call an invalid direct-API interval scientifically justified.
4. Run complete scientific gates before native C2 timing; accept or revert,
   then reprofile. C3 exact row-endpoint rejection is a separate candidate,
   not yet implemented. Its corrected shadow probe measures about 45.45% net
   reduction in Z predicates, not a native speedup. See the C3 proposal and
   `evidence/corrected_occupancy_shadow/`.
5. Recheck the final combined build against the same corrected baseline and
   independent references, review all workload regressions, publish the full
   report/evidence, then commit and push. Existing user authorization persists.

Reusable controllers are preserved under `controllers/`,
`candidate_reference_controller_v1/`, `candidate_reference_resolution_v1/`,
`candidate_diagnostic_controller_v1/` and `candidate_reprofile_controller_v1/`.
Always use new destinations and preserve executing helper bytes. Native timing
windows must not overlap builds, reference calculations or compression.

Publish compact exact-byte evidence with content-addressed compressed blobs and
a restoration index; preserve raw timestamps, failures and all native samples.
Reference shared baseline evidence rather than copying its corpus. Keep executable
binaries local with hashes and reproducible source/build instructions. Do not
stage user media. The final report remains
`docs/performance/SCIENTIFICALLY_EXACT_OPTIMIZATION.md`.

# Final accepted-C2 publication preparation

Preparation only during final native timing. No traversal, artifact hashing,
compression, verification, storage tests, or assembly is authorized until root
announces the quiet window ended. The copied shared storage/controller files are
unexecuted drafts; final source/test identities will be frozen afterwards.

Use storage format 2 and the existing accepted-candidate coverage contract. The
new `final_acceptance_v1/acceptance.json` must be a real
`accepted_corrected_optimization`, accepted true, with the SAME existing C2 build
and native executable. This is final re-admission of unchanged C2, not C3 or a new
algorithm. Do not fabricate an acceptance-shaped object from another receipt.

Required primary evidence roots (explicit, no mutable prefix glob):

- `final_acceptance_v1`
- `final_performance_gate_v1`
- `final_native_pairs_v1` (40 configurations, 720 launches, 320 measured pairs,
  80 warmup processes; all ten workloads at 1/2/4/6 threads)
- `final_native_independent_review_v1`
- `final_post_optimization_v1` including all original reference comparison
  failures plus `independent_reference_resolution_v1`
- `final_command_boundary_v1` (the same31 cases; no new expected output)
- `final_supplemental_checks_v1`
- `final_engineering_v1`
- `final_controller_hardening_v1`
- actual final measurement publication receipt directory, supplied after execution
- this publication's own frozen controllers/tests/spec/closure and logs

External raw roots must be listed explicitly:
`.stericx/scientific_remediation/cli_v1/runs/corrected_vFinal_t1`, `_t2`, `_t4`,
`_t6`. Reuse existing immutable 31-case plan, corrected baseline capture and
boundary controller; prior accepted-C2 full prediction bitstreams and their
source/native exporter proof; scientific/input source snapshots and all required
fixture files through pinned providers. No binaries, targets, virtual environments,
site-packages, media directories or external symlinks.

Provider packages: accepted_c1, accepted_c1b, accepted_c2, rejected_c3. Pin manifest,
restoration index and compressed plan for each. Rejected-C3 is a byte provider
only: its accepted=false decision stays separate and cannot supply final admission.

Freeze only helpers proven actually executed by the final receipts:
run_final_observations.py; run_supplemental_checks_v2.py; replay_cli_bridge.py;
run_final_engineering.py; assemble_candidate_gate.py; the final acceptance helper
whose actual name will come from the receipt; publish_final_measurements.py ONLY
AFTER its execution; reference/matrix reviewers and imported helper records
already archived inside their executed roots. No broad `controllers/` glob.
Preserve original hardening attempts and reviews alongside their successors.

Final documents must settle before their bytes are pinned: README.md; main
SCIENTIFICALLY_EXACT_OPTIMIZATION.md; POST_OPTIMIZATION_SCIENTIFIC_EQUIVALENCE.md;
FINAL_NATIVE_MEASUREMENTS.md; current_final_measurements.json; final result/review
files actually emitted by the publisher. New publication README and full verifier/
restorer source are added as exact readable copies after core package completion.

Additional final coverage requirements, beyond unchanged native/13-lane/four-CLI
accepted storage checks:

1. All receipts bind the same C2 build82b63f16/nativeb2335016 and legitimate final
   acceptance; prior acceptance071f29... remains an explicit prior binding.
2. Actual final native analysis has40 configurations/720 launches/320pairs,
   no exclusions, all measured pairs and exact scientific fingerprints; analysis,
   native manifest/summary and final gate match final acceptance references.
3. Fresh observation completion retains all commands/logs, exactly13 lanes and
   128021responses; original eight inferred errors/topology successes unchanged.
4. Final independent gate passes all25 commands under unchanged equations and
   limits; original six-timestamp failed gate/resolution remain. 7624model checks
   exact; no new numerical tolerance or global-science assertion.
5. Final31 compare/direct-volume/model/DB/predict capture must be tied to the
   original frozen plan/baseline and accepted-C2 build; rehash and recompute exact
   stdout/stderr/artifact fingerprints under only the already named metrics.
   Preserve all18successes/13errors and all required output files.
6. Final supplemental24 CLI cases, four integrity tests and Study011 verify-only
   retain actual command status/raw artifacts. Same native/query/DB bindings;
   no normalization for the24 CLI cases.
7. Engineering has eight expected command names, all exit0; source/test/log
   bindings and measured versus delivered release bytes equal acceptedC2.
   Later documentation checks are separate, not a claim that old logs ran on
   future Markdown edits.
8. Final publisher binds actual native analysis and all40 public rows, no dropped
   unfavorable samples. C3 rejection/source restoration and all known scientific,
   method and predictive limitations remain visible.
9. Full verification, fresh restoration, unchanged31 storage tests plus focused
   new final-coverage tests, and independent review must pass before final seal.

No final package plan/build will run until all required receipts and final report
bytes exist and root releases the quiet window.

Final report cleanup after receipt finalization: replace the preliminary coverage
intro with fulfilled finite scopes; distinguish completed publication/restoration
from optional reproduction commands; final replay used unchanged C2 while the
separate engineering release rebuild reproduced exactly the same bytes.

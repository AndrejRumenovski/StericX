{
  "argv": [
    "/run/media/andrej-rumenovski/New Volume/Code/StericX/docs/benchmarks/morfeus_current/environment/venv/bin/python",
    "-c",
    "import morfeus,pathlib,hashlib,json; p=pathlib.Path(morfeus.__file__).parent; print(json.dumps({str(f.relative_to(p)):hashlib.sha256(f.read_bytes()).hexdigest() for f in sorted(p.rglob('*.py'))},indent=2))"
  ],
  "returncode": 0
}

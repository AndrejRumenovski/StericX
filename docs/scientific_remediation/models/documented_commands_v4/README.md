# Final v4 documented commands

The [receipt](receipt.json) records all 17 native commands from the current
reaction-screening and model-format tutorials succeeding against the frozen
corrected v4 binary. Source documents, executable, build manifest, corrected
metadata bundle, logs and generated outputs are hashed; input identities were
checked before and after execution. The full fresh output tree is
`.stericx/scientific_remediation/demo/documented_commands_v4`.

The `screening` and `model_format` subdirectories keep their distinct documented
partitions and write new packed records and fitted models. Both declare supplied
weight means at response temperature 353.15 K, retaining the historical supplied
populations from 298.15 K. No old fitted model or scientific audit artifact was
overwritten. Command success establishes runnable examples, not empirical
validation, calibration or optimization acceptance. Earlier v2/v3 receipts and
their schema/refusal checks remain separate evidence.
